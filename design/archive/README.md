# Historical design archive

This directory preserves the week-by-week planning and status trail that accompanied the initial `agent-terminal` v1 build-out.
These files remain useful for historical context, but they are no longer the primary entry point for understanding the current product surface.

Use [`../README.md`](../README.md) for the current design index and [`../ARCHITECTURE.md`](../ARCHITECTURE.md) for the stable overview.
